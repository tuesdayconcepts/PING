"use client";

import AdminPage from "@/legacy/pages/AdminPage";

// Client-only wrapper: the legacy admin dashboard is fully client-side.
export default function LegacyAdminApp() {
  return <AdminPage />;
}

