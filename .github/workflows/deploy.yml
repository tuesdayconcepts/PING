name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # NOTE: This repo previously deployed `client/` to Netlify and `server/` to Railway.
  # The app is now a single Next.js deployment on Vercel, so CI deploy steps were removed
  # to avoid failing workflows when those integrations are gone.
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Deploy is handled by Vercel for this repository."

